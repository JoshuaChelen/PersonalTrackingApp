# Supabase — fill these in from your Supabase project (Settings -> API).
# These are EXPO_PUBLIC_* so they are embedded in the client bundle (the anon
# key is public by design). Leave them blank to run the app fully local-only.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
